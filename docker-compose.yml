services:
  imu:
    build: .
    container_name: smart-mfg-imu
    privileged: true
    devices:
      - "/dev/i2c-1:/dev/i2c-1"
      - "/dev/gpiomem:/dev/gpiomem"
    network_mode: host
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    command: ["python3", "-m", "imu"]
